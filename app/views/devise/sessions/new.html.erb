<div class="sign-in small-10 small-offset-1 large-4 large-offset-4" id = "sign-up">
  <h2 class="text-center">Log In</h2>
  <%= simple_form_for(resource, as: resource_name, url: session_path(resource_name)) do |f| %>
    <div class="form-inputs">
      <%= f.input :login, required: false, autofocus: true, label: "Username or Email Address" %>
      <%= f.input :password, required: false %>
      <%= f.input :remember_me, as: :boolean if devise_mapping.rememberable? %>
    </div>

    <div class="form-actions text-center">
      <%= f.button :submit, "Log in", class: 'button radius expand' %>
    </div>
  <% end %>
  <br />
  <p> New here? <%= render "devise/shared/links" %></p>
</div>

