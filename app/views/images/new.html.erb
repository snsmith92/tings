<%= simple_form_for @image, url: ad_images_path(@ad) do |f| %>
  <%= f.input :caption %>
  <button type="button" class="btn btn-secondary" data-dismiss="modal">Close</button>
  <%= f.submit "Add Photo", class: 'button' %>
<% end %>